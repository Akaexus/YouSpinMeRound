// Napisz program wyznaczający wartość funkcji kwadratowej y = ax2 + bx + c w
// zadanym punkcie. Wartości współczynników a, b, c należy wczytać z klawiatury.

#include <iostream>

double $(char __ = 0) {
  double _;
  std::cout << __ << '>';
  while (!(std::cin >> _)) {
    std::cout << __ << '>';
    std::cin.clear();
    std::cin.ignore();
  }
  return _;
}
double f(double __, double _, double ___, double _______) {
  return ___ + _ * _______ + __ * _______ * _______;
}
int main() {
  std::cout << f($('a'), $('b'), $('c'), $('x'));
  return 0;
}
